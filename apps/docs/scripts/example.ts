/**
 * Returns the average of two numbers.
 *
 * @remarks
 * This method is part of the {@link core-library#Statistics | Statistics subsystem}.
 *
 * @param param1: string | number = "123" - The first input number.
 * multiple lines line for x.
 * @param param2: string - The second ..............
 * @param breadcrumb: "/" | "&gt;" = "/" - The  breadcrumbs..............
 * With special chars:
 * function getAverage(x, y) {
 *   return (x + y) / 2.0;
 * }
 * `<code>abc</code>`
 * `() => void`
 * `{a:1}`D
 * @param y: Function - The y ..............
 * @param z = "abc" - The z param ..............
 * @param w - The w param is (sum) of x and z . {}
 * 
 * 
 * @param media: slot - For slot table. The image slot for image button. `<br />`
 * Ex: `&lt;img slot=&quot;media&quot; src=&quot;https://picsum.photos/200/300&quot; /&gt;`
 * 
 * 
 * @returns The arithmetic mean of x and z
 *
 * @beta
 */

function a(x = 1) {
  console.log(1);
}
