---
import { fetchVersions } from "../_api/fetchVersions"

const versions = await fetchVersions();
// import "@studs/ui/inputs/dropdown";
---

<!-- <studs-dropdown
  label="Select version"
  options={versions}
  value={versions[0]}
  onChange={(e) => console.log(e.detail)}></studs-dropdown> -->